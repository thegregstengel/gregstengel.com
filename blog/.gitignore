# Bundler cache
.bundle
vendor
# Gemfile.lock is intentionally committed for reproducible CI builds
# (this repo is deployed as a site, not published as a gem). See CLAUDE.md.

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist
